# ИНВАРИАНТЫ И ЗАЩИТА ОТ НЕКОРРЕКТНЫХ СОСТОЯНИЙ РЫНКА

**Дата:** 2024-12-19  
**Задача:** Добавить инварианты и защиту от некорректных типов MarketState

---

## ✅ ВЫПОЛНЕНО

### Добавлены инварианты в ключевых точках системы:

1. ✅ **Перед расчётом риска** (`risk.py`)
2. ✅ **Перед расчётом скоринга** (`scoring.py`)
3. ✅ **Перед формированием сигнала** (`signals.py`)
4. ✅ **В функции нормализации** (`core/market_state.py`)

---

## 📝 ДОБАВЛЕННЫЕ ИНВАРИАНТЫ

### 1. core/market_state.py

#### Функция `validate_state()`
**Назначение:** Проверка инварианта для одного значения состояния

**Защита:**
- Проверяет, что значение - `MarketState` enum или `None`
- Логирует ERROR при обнаружении невалидного типа
- Безопасный fallback: пытается нормализовать строку, иначе возвращает `None`
- Не падает, но фиксирует проблему

**Код:**
```python
def validate_state(state, context: str = "") -> Optional[MarketState]:
    """
    Проверяет инвариант: state должен быть MarketState enum или None.
    
    КРИТИЧНО: MarketState - ключевая доменная сущность.
    Ошибка типа (str вместо enum) - архитектурная ошибка, которая может
    привести к неправильным торговым решениям.
    """
    if state is None:
        return None
    
    if isinstance(state, MarketState):
        return state
    
    # НЕВАЛИДНЫЙ ТИП - логируем ERROR
    logger.error(f"Invalid market state type: {state} (type: {type(state).__name__})")
    
    # Безопасный fallback
    if isinstance(state, str):
        normalized = normalize_state(state)
        if normalized is not None:
            return normalized
    
    return None  # Безопасный fallback
```

#### Обновлена `normalize_states_dict()`
**Изменение:** Использует `validate_state()` для проверки каждого значения

**Защита:**
- Автоматически проверяет инварианты при нормализации
- Логирует ошибки, если нормализация не сработала
- Гарантирует, что выходной словарь содержит только `MarketState` или `None`

---

### 2. risk.py

#### Функция `risk_level()`
**Инвариант:** После нормализации все значения должны быть `MarketState` enum или `None`

**Защита:**
- Проверяет каждое значение в словаре `states` после нормализации
- Логирует ERROR при обнаружении невалидного типа
- Заменяет невалидные значения на `None` (безопасный fallback)
- Не падает, но фиксирует архитектурную ошибку

**Код:**
```python
# ИНВАРИАНТ: Проверяем, что после нормализации все значения - MarketState enum или None
# КРИТИЧНО: Ошибка типа здесь может привести к неправильной оценке риска
# и принятию опасных торговых решений
for key, state in states.items():
    if state is not None and not isinstance(state, MarketState):
        logger.error(
            f"INVARIANT VIOLATION in risk_level: state[{key}] = {state} (type: {type(state).__name__}), "
            f"expected MarketState or None. This is an architectural error."
        )
        states[key] = None  # Безопасный fallback
```

**Защищает от:**
- Неправильной оценки риска из-за некорректного типа
- Принятия опасных торговых решений
- Тихих ошибок, которые могут привести к убыткам

---

### 3. scoring.py

#### Функция `calculate_score()`
**Инвариант:** После нормализации все значения должны быть `MarketState` enum или `None`

**Защита:**
- Проверяет каждое значение в словаре `states` после нормализации
- Логирует ERROR при обнаружении невалидного типа
- Заменяет невалидные значения на `None` (безопасный fallback)

**Код:**
```python
# ИНВАРИАНТ: Проверяем, что после нормализации все значения - MarketState enum или None
# КРИТИЧНО: Ошибка типа здесь может привести к неправильному скорингу
# и принятию неоптимальных торговых решений
for key, state in states.items():
    if state is not None and not isinstance(state, MarketState):
        logger.error(
            f"INVARIANT VIOLATION in calculate_score: state[{key}] = {state} (type: {type(state).__name__}), "
            f"expected MarketState or None. This is an architectural error."
        )
        states[key] = None  # Безопасный fallback
```

**Защищает от:**
- Неправильного скоринга из-за некорректного типа
- Принятия неоптимальных торговых решений
- Пропуска хороших возможностей или открытия плохих позиций

#### Функция `get_entry_conditions()`
**Инвариант:** После нормализации все значения должны быть `MarketState` enum или `None`

**Защита:**
- Аналогична `calculate_score()`
- Проверяет инварианты перед определением условий входа

**Защищает от:**
- Неправильного определения условий входа
- Открытия позиций в неподходящих условиях

---

### 4. signals.py

#### Функция `build_signal()`
**Инвариант:** После нормализации все значения должны быть `MarketState` enum или `None`

**Защита:**
- Проверяет каждое значение в словаре `states` после нормализации
- Логирует ERROR при обнаружении невалидного типа
- Заменяет невалидные значения на `None` (безопасный fallback)

**Код:**
```python
# ИНВАРИАНТ: Проверяем, что после нормализации все значения - MarketState enum или None
# КРИТИЧНО: Ошибка типа здесь может привести к формированию некорректного сигнала
# и отправке неправильной информации трейдерам
for key, state in states.items():
    if state is not None and not isinstance(state, MarketState):
        logger.error(
            f"INVARIANT VIOLATION in build_signal: state[{key}] = {state} (type: {type(state).__name__}), "
            f"expected MarketState or None. This is an architectural error."
        )
        states[key] = None  # Безопасный fallback
```

**Защищает от:**
- Формирования некорректного сигнала
- Отправки неправильной информации трейдерам
- Введения пользователей в заблуждение

---

## 🛡️ ЗАЩИТА ОТ ОШИБОК

### Что ловится:

1. **Строки вместо enum:**
   ```python
   states = {"15m": "D"}  # Строка вместо MarketState.D
   # → Ловится в normalize_states_dict() и validate_state()
   # → Логируется ERROR
   # → Нормализуется или заменяется на None
   ```

2. **Невалидные типы:**
   ```python
   states = {"15m": 123}  # Число вместо enum
   # → Ловится в validate_state()
   # → Логируется ERROR
   # → Заменяется на None
   ```

3. **Некорректные значения после нормализации:**
   ```python
   # Если normalize_states_dict() не сработала правильно
   states = {"15m": "X"}  # Невалидная строка
   # → normalize_state("X") вернёт None
   # → Проверка инварианта в runtime-функциях зафиксирует проблему
   ```

### Поведение при ошибке:

1. **Логирование ERROR:**
   - Тип ошибки
   - Контекст (функция, ключ)
   - Ожидаемый тип

2. **Безопасный fallback:**
   - Замена на `None` вместо падения
   - Система продолжает работать
   - Торговые решения принимаются с учётом отсутствия данных

3. **Не падает:**
   - Fail-safe подход
   - Система остаётся работоспособной
   - Проблема фиксируется для последующего анализа

---

## 📊 КАКИЕ ОШИБКИ ТЕПЕРЬ ЛОВЯТСЯ РАНЬШЕ

### До добавления инвариантов:
- ❌ Ошибки типов могли проскочить незамеченными
- ❌ Неправильные торговые решения без логирования
- ❌ Тихие ошибки, которые сложно отладить

### После добавления инвариантов:
- ✅ Ошибки типов ловятся сразу после нормализации
- ✅ Все нарушения контракта логируются как ERROR
- ✅ Система сигнализирует о проблеме, но не падает
- ✅ Упрощается отладка (видно, где именно нарушен контракт)

---

## 🎯 ПРЕИМУЩЕСТВА

1. **Раннее обнаружение ошибок:**
   - Ошибки ловятся сразу, а не в момент использования
   - Легче найти источник проблемы

2. **Безопасность:**
   - Система не падает при ошибках типов
   - Безопасный fallback гарантирует работоспособность

3. **Отладка:**
   - Явные логи с контекстом
   - Видно, где именно нарушен контракт

4. **Документация:**
   - Инварианты явно задокументированы в коде
   - Комментарии объясняют, почему проверка критична

---

## 📝 ПРИМЕРЫ ЛОГИРОВАНИЯ

### Пример 1: Строка вместо enum
```
ERROR: Invalid market state type detected: D (type: str) in context: normalize_states_dict[15m]
WARNING: Auto-normalized string 'D' to MarketState enum (should be normalized earlier)
```

### Пример 2: Невалидный тип
```
ERROR: INVARIANT VIOLATION in risk_level: state[15m] = 123 (type: int), expected MarketState or None. This is an architectural error.
```

### Пример 3: Невалидная строка
```
ERROR: Invalid market state type detected: X (type: str) in context: normalize_states_dict[15m]
WARNING: Could not normalize state 'X', using None as fallback
ERROR: INVARIANT VIOLATION in calculate_score: state[15m] = None (type: NoneType), expected MarketState or None. This is an architectural error.
```

---

## ✅ РЕЗУЛЬТАТ

### Достигнуто:
1. ✅ **Инварианты добавлены** во все ключевые точки
2. ✅ **Ошибки типов ловятся сразу** после нормализации
3. ✅ **Система сигнализирует** о нарушении контракта
4. ✅ **Безопасный fallback** гарантирует работоспособность
5. ✅ **Упрощена отладка** через явное логирование

### Защита от:
- ❌ Строк вместо enum в runtime-логике
- ❌ Невалидных типов (числа, объекты и т.д.)
- ❌ Тихих ошибок, которые сложно отладить
- ❌ Неправильных торговых решений из-за ошибок типов

---

*Инварианты добавлены и готовы к использованию.*

