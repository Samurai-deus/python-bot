# 🚀 Запуск бота в фоновом режиме

## ⚠️ ВАЖНО: Если запускаете через `python runner.py` в терминале

**НЕТ, нельзя просто закрыть терминал!** Процесс будет убит при закрытии терминала.

## ✅ Правильные способы запуска:

### 1. **Systemd Service (РЕКОМЕНДУЕТСЯ)** - Автозапуск при перезагрузке

```bash
# Скопировать service файл
sudo cp market-bot.service /etc/systemd/system/

# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable market-bot

# Запустить бота
sudo systemctl start market-bot

# Проверить статус
sudo systemctl status market-bot

# Посмотреть логи
sudo journalctl -u market-bot -f

# Остановить
sudo systemctl stop market-bot
```

**Преимущества:**
- ✅ Автоматический запуск при перезагрузке сервера
- ✅ Автоматический перезапуск при падении
- ✅ Работает в фоне независимо от терминала
- ✅ Управление через systemctl

---

### 2. **Nohup** - Простой способ

```bash
nohup python runner.py > bot.log 2>&1 &
```

**Проверить процесс:**
```bash
ps aux | grep runner.py
```

**Остановить:**
```bash
pkill -f runner.py
```

**Преимущества:**
- ✅ Просто запустить
- ✅ Работает после закрытия терминала

**Недостатки:**
- ❌ Не перезапускается при перезагрузке сервера
- ❌ Нужно вручную запускать после перезагрузки

---

### 3. **Screen** - Для временного запуска

```bash
# Установить screen (если нет)
sudo apt install screen

# Создать новую сессию
screen -S market_bot

# Запустить бота
python runner.py

# Отключиться от сессии (бот продолжит работать)
# Нажать: Ctrl+A, затем D

# Вернуться к сессии
screen -r market_bot

# Посмотреть все сессии
screen -ls
```

**Преимущества:**
- ✅ Можно вернуться и посмотреть вывод
- ✅ Работает после закрытия терминала

**Недостатки:**
- ❌ Не перезапускается при перезагрузке

---

### 4. **Tmux** - Альтернатива screen

```bash
# Установить tmux (если нет)
sudo apt install tmux

# Создать новую сессию
tmux new -s market_bot

# Запустить бота
python runner.py

# Отключиться (бот продолжит работать)
# Нажать: Ctrl+B, затем D

# Вернуться к сессии
tmux attach -t market_bot
```

---

## 📊 Проверка работы бота

### Проверить, что бот работает:

```bash
# Через systemd
sudo systemctl status market-bot

# Через процессы
ps aux | grep runner.py

# Через логи
tail -f runner.log
```

### Проверить в Telegram:

Отправьте команду `/status` боту - он должен ответить.

---

## 🔄 Рекомендация

**Используйте Systemd Service** - это самый надежный способ:
- Автоматический запуск при перезагрузке
- Автоматический перезапуск при ошибках
- Удобное управление
- Централизованные логи

