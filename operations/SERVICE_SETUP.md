# 🔧 Установка Market Bot как Systemd Service

## 📋 Пошаговая инструкция

### 1. Убедитесь, что вы в директории проекта

```bash
cd /root/market_bot
```

### 2. Установите service (выберите один способ)

#### Способ A: Автоматический скрипт (РЕКОМЕНДУЕТСЯ)

```bash
# Сделать скрипт исполняемым
chmod +x setup_service.sh

# Запустить с правами root
sudo bash setup_service.sh
```

#### Способ B: Вручную

```bash
# 1. Скопировать service файл
sudo cp market-bot.service /etc/systemd/system/

# 2. Перезагрузить systemd
sudo systemctl daemon-reload

# 3. Включить автозапуск
sudo systemctl enable market-bot

# 4. Запустить бота
sudo systemctl start market-bot
```

---

## 🎮 Управление ботом

### Запуск
```bash
sudo systemctl start market-bot
```

### Остановка
```bash
sudo systemctl stop market-bot
```

### Перезапуск
```bash
sudo systemctl restart market-bot
```

### Статус
```bash
sudo systemctl status market-bot
```

### Проверка, что бот работает
```bash
# Посмотреть процессы
ps aux | grep runner.py

# Или проверить в Telegram
# Отправьте команду /status боту
```

---

## 📊 Просмотр логов

### Логи systemd (рекомендуется)
```bash
# Последние логи
sudo journalctl -u market-bot -n 50

# Логи в реальном времени
sudo journalctl -u market-bot -f

# Логи за сегодня
sudo journalctl -u market-bot --since today
```

### Логи бота (runner.log)
```bash
tail -f /root/market_bot/runner.log
```

---

## ✅ Проверка работы

### 1. Проверить статус service
```bash
sudo systemctl status market-bot
```

Должно быть:
- `Active: active (running)`
- Зеленый статус

### 2. Проверить в Telegram
- Отправьте `/status` боту
- Должен ответить с текущим статусом

### 3. Проверить процессы
```bash
ps aux | grep runner.py
```

Должен быть процесс `python runner.py`

---

## 🔄 Автозапуск при перезагрузке

Service автоматически запустится при перезагрузке сервера, так как мы выполнили:
```bash
sudo systemctl enable market-bot
```

Проверить:
```bash
sudo systemctl is-enabled market-bot
# Должно вернуть: enabled
```

---

## 🛠️ Устранение проблем

### Бот не запускается

1. Проверить логи:
```bash
sudo journalctl -u market-bot -n 100
```

2. Проверить права доступа:
```bash
ls -la /root/market_bot/runner.py
```

3. Проверить путь к Python:
```bash
/root/market_bot/venv/bin/python --version
```

### Бот падает

1. Проверить логи:
```bash
sudo journalctl -u market-bot -f
```

2. Проверить файл runner.log:
```bash
tail -f /root/market_bot/runner.log
```

3. Проверить, что все зависимости установлены:
```bash
cd /root/market_bot
source venv/bin/activate
python -c "import telegram; print('OK')"
```

### Отключить автозапуск
```bash
sudo systemctl disable market-bot
```

### Удалить service
```bash
sudo systemctl stop market-bot
sudo systemctl disable market-bot
sudo rm /etc/systemd/system/market-bot.service
sudo systemctl daemon-reload
```

---

## 📝 Важные замечания

1. **Service работает от имени root** - это нормально для системного сервиса
2. **Логи сохраняются** в `runner.log` и в systemd journal
3. **Автоматический перезапуск** - если бот упадет, systemd перезапустит его через 10 секунд
4. **Мониторинг** - используйте `/status` в Telegram для проверки работы

---

## 🎯 Готово!

После установки бот будет:
- ✅ Работать в фоне
- ✅ Автоматически запускаться при перезагрузке
- ✅ Автоматически перезапускаться при падении
- ✅ Логироваться в systemd journal

Можно спокойно закрывать терминал! 🚀

