# Техдолг исследовательской программы (открытые пункты)

Правило: пункт закрывается PR со ссылкой; у каждого — как проверить, что он ещё открыт.

## Исполнители (portfolio/, btcalts/, carry/)

- **Ребалансировка в неделю запуска перезаписывала запись понедельника.** 14.09 второй прогон И14 заменил двуногую запись (ключ `rebalances.t` — понедельник); история спасена бэкапом. 15.09 добавлен журнал `rebalance_runs` (все прогоны), `rebalances` хранит последний. Долг: отчёты и API читают только `rebalances` — показать повторы из журнала.
  Verify: `grep -n rebalance_runs api/research_data.py portfolio/report.py` — пусто → открыт.
- ~~Демо-средства на пустом субсчёте — путь не проверен вживую~~ — 15.09 проверено на субсчёте И18: `apply_demo_usdt(1000)` → доступно +1 002 USDT (до 198 944, после 199 946); на правило не влияет.
- ~~**Демо-средства на пустом субсчёте — путь не проверен вживую.**~~ Bybit выдал И18 100 000 USDT сам; `ensure_funds`/`apply_demo_usdt` покрыты только тестами с подделкой.
  Verify: `docker exec market-bot-btcalts python -c "import sqlite3; print(sqlite3.connect('/btcalts/btcalts.db').execute(\"select count(*) from events where kind='demo_funds'\").fetchone())"` → (0,) — путь не срабатывал.
- ~~Список не-крипты собран по именам~~ — 15.09: у биржи есть признак — `symbolType` (stock / ETF / commodity / forex; пусто у крипты, `innovation` — крипта) и `marketRegion`; `is_crypto_instrument` отсеивает по нему в `launch_ages_d` (кандидаты И14/И18) и в кэше `history_wide`; имена — вторая сетка. Проверено 15.09: в позициях И14/И18 не-крипты нет.
- ~~**Список не-крипты (`backtest/wide_search.NON_CRYPTO_BASES`) собран по именам**~~ и неполон по построению (SKHYNIX всплыл 15.09). Динамическая блокировка по ошибке 110126 есть у И18 и ноги И14; в бэктестах новых имён не будет видно. Долг: источник признака «токенизированный актив» от биржи (если появится) или проверка кандидатов пробным запросом при формировании вселенной.
  Verify: `py -c "from backtest.wide_search import NON_CRYPTO_BASES as n; print(len(n))"` — список всё ещё ручной.
- **Час исполнения ребалансировок — 00:02 UTC понедельника, самый тонкий час** (атлас: 4 % суточного оборота против 7,6 % в 14:00). Решение — 12.10 по замеру проскальзывания четырёх ребалансировок; переносить вместе с бумагой.
- ~~Календарь уведомлений дублирует даты плана вручную~~ — 15.09: прямой и обратный тесты (`test_notification_section_dates_are_in_the_calendar`).

## Бэктесты (backtest/)

- ~~`history.sync_funding` перечитывает фандинг целиком при каждом повторе~~ — закрыто 15.09: `tail_days` в `sync_funding`, по умолчанию 3 дня в `history_wide universe` (полный перечит — `--tail-days 0`).
- ~~Загрузчик широкой вселенной живёт вне репо~~ — закрыто 15.09: `backtest/history_wide.py` (`universe`, `hourly`), кэш описан в README.
- ~~Просадка портфеля в `bar_rules` при неограниченном числе позиций~~ — закрыто 15.09: `cap_concurrency` и `--max-open`; итоги И16 не пересчитывались (вердикт от этого не зависел).
- ~~PROGRAM в `api/research_data.py` повторяет тексты плана вручную~~ — 15.09: тест `test_program_dates_are_in_the_plan` ловит расхождение дат; тексты по-прежнему вручную (осознанно).

## Мини-апп

- После v2 остались неиспользуемые части: зависимость `lightweight-charts`, `fetchMonthlyTarget`/`fetchAnalytics*` в `api/endpoints.ts`, роутеры `analytics`/`positions`/`signals` в API (фронт их не зовёт). Долг: убрать или пометить как «для Telegram-бота».
- ~~Экраны глазами — только из Telegram~~ — 15.09: `npm run dev:mock` (vite --mode mock → `VITE_MOCK=1` из vite.config, только dev-сервер) — экраны из фикстур `src/api/fixtures` (живой снимок API); форму фикстуры сверяет `test_miniapp_fixture_has_the_shape_of_the_api_response`. Обхода авторизации на проде нет.

## Данные

- **Разлоки и эмиссия токенов** — механизм «альты истекают к BTC» (атлас, И18) не подтверждён данными об предложении. Долг: еженедельная запись circulating supply по всем контрактам (CoinGecko) — 3–4 месяца до первой проверки.

## План закрытия (записан 15.09.2026; порядок — по риску и пользе, каждый пункт — свой PR с тестами)

**Волна 1 — код и тесты, без выкладки (≈ 3 ч):**
1. `rebalance_runs` в отчётах `portfolio.report`/`btcalts.report` и в `/api/research/overview` → мини-апп показывает повторы прогонов (30 мин).
2. Мини-апп: убрать мёртвое — `lightweight-charts`, `fetchMonthlyTarget`/`fetchAnalytics*`/`fetchSignals*` из `endpoints.ts`, типы; API-роутеры `analytics`/`positions`/`signals` оставить (их читает Telegram-бот? — проверить grep, если нет — удалить вторым шагом) (40 мин).
3. `backtest/history_wide.py` — CLI загрузки широкой вселенной (то, что лежит в scratchpad: инструменты, 4h от листинга, 1h для top-N) + режим докачки фандинга «только хвост» с параметром, полный перечит — по флагу; документация: кэш `history_wide.db` живёт локально (путь в README/плане) (1,5 ч).
4. Тест «даты PROGRAM (api/research_data) есть в плане» + обратный тест «даты раздела «Уведомления» плана есть в календаре» (30 мин).
5. `bar_rules`: параметр «не больше N одновременных позиций» и просадка на капитал с этим ограничением — для будущих правил по сделкам; текущие итоги И16 не пересчитываются (1 ч).

**Волна 2 — требует выкладки или действия на бирже (слово владельца, ≈ 2,5 ч):**
6. Проверка пути демо-средств вживую: одноразовый запрос `apply_demo_usdt(1000)` на субсчёте И18 с проверкой баланса до/после (15 мин; демо-деньги, на правило не влияет).
7. Признак «токенизированный актив» от биржи: разобрать поля `instruments-info` (30 мин исследования); если поля нет — оставить блокировку по 110126 и добавить предпроверку кандидатов до ребалансировки (пробный запрос статуса контракта) (1 ч).
8. Просмотр мини-аппа в браузере без Telegram: режим фикстур `VITE_MOCK=1` в dev-сборке (данные из JSON, без прода и без обхода авторизации) (1 ч). Обход авторизации на проде не делать.
9. Еженедельная запись circulating supply по всем контрактам (CoinGecko, бесплатный API) в базу бота — сборщик в контейнере новостей; через 3–4 месяца — данные под механизм И18 (2 ч + выкладка).

**Волна 3 — по данным, не раньше дат:**
10. Час исполнения ребалансировок — решение 12.10 по замеру проскальзывания 4 ребалансировок (в календаре).
11. Отчёты `rebalance_runs` — после п. 1; PROGRAM из плана генерировать не будем (тест п. 4 достаточен).

**Что нужно от владельца:** слово на выкладку волны 2 (одна выкладка на всё) и согласие на п. 6 (запрос демо-средств на субсчёте И18).
