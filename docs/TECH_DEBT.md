# Техдолг исследовательской программы (открытые пункты)

Правило: пункт закрывается PR со ссылкой; у каждого открытого — как проверить, что он ещё открыт.
Закрытое сюда не возвращается: история правок — в git и в журнале `docs/TRADER_PLAN.md`.

## Открыто

- **Час исполнения ребалансировок — 00:02 UTC понедельника, самый тонкий час** (атлас: 4 % суточного оборота против 7,6 % в 14:00). Решение — 12.10.2026 по замеру проскальзывания четырёх ребалансировок И14; переносить вместе с бумагой (правило и календарь — одним PR).
  Verify: дата в `portfolio/calendar.py` (`2026-10-12`) ещё в будущем → открыт.
- **Вердикт И19б — 16.11.2026** на 8 неделях записи межбиржевого фандинга. Не прошло — запись останавливается, контейнер новостей перестаёт снимать три биржи.
  Verify: `ssh … "docker exec market-bot-news python -c \"import sqlite3; c=sqlite3.connect('/data/db/xfunding_live.db'); print(c.execute('SELECT COUNT(DISTINCT hour_ms) FROM snap').fetchone())\""` — часы копятся → открыт.
- **Сбор предложения монет (`coin_supply`) — данные под механизм И18.** Гипотеза записывается не раньше 16 недель сбора (середина января 2027), до этого таблица не читается.
  Verify: `ssh … "docker exec market-bot-news python -c \"import database; c=database.get_db_connection(); cur=c.cursor(); cur.execute('SELECT COUNT(DISTINCT week_ms) FROM coin_supply'); print(cur.fetchone())\""` — недель < 16 → открыт.
- **Учёт исполнителя не отличает внешний приток средств от торговли** (`python -m portfolio.adjust` — вручную). Автоматика (сверка стоимости с журналом сделок и фандинга) не нужна, пока счета демо и пробы редки. Решение — при переходе на реальные деньги (план, Ф7).
- **Перед включением сигнальной торговли** (`SIGNAL_TRADING_ENABLED=true`) вернуть `BOT_INTERVAL=300` и проверить состояние ручной паузы бота (`manual_pause_active`): пауза снимается командой `/resume`, сама она снимается только через восстановление из SAFE_MODE.
  Verify: `ssh … "docker exec market-bot sh -c 'env | grep -E \"SIGNAL_TRADING_ENABLED|BOT_INTERVAL\"'"`.

## Закрыто 23.09.2026 (PR закрытия техдолга)

- ~~Размер позиций бота считался от equity общего с И14 счёта~~ — `capital.own_capital()`: потолок плюс результат закрытых сделок бота; `get_current_balance` берёт его, когда счёт отдан портфелю (`SIGNAL_TRADING_ENABLED=false`).
- ~~Снимки и отчёты считали позиции по цене входа~~ — `positions_usdt` считает по текущей цене (вход + нереализованный результат), без лишних запросов к бирже.
- ~~Отправка сообщения о резком движении не писала в журнал~~ — теперь пишет символ, таймфрейм и процент; проверено на данных: за 7 дней по 7 монетам правило дало бы 5 сообщений (окно повтора — 30 минут на символ и таймфрейм).
- ~~Пауза торговли от CRITICAL-алерта при выключенных сигналах~~ — пауза ставится только когда сигнальная торговля включена.
- ~~Адрес `instruments-info` повторялся в четырёх модулях~~ — один источник `exchange.bybit_client.instruments_url()`.
- ~~Мёртвые роутеры API (`positions`, `signals`, `analytics`)~~ — удалены вместе с регистрацией: мини-апп v2 их не звал, Telegram-бот ходит в базу напрямую, smoke и мониторинг их не трогали.
- ~~Долг «отчёты и API читают только `rebalances`»~~ — закрыт ещё 15.09 (`rebalance_runs` в `portfolio.report`, `btcalts.report`, `/api/research/overview`); запись в этом файле устарела.
- ~~Мёртвые части мини-аппа (`lightweight-charts`, лишние endpoints)~~ — удалены 15.09 в волне 1; запись устарела.
