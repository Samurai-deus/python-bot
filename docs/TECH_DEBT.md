# Техдолг исследовательской программы (открытые пункты)

Правило: пункт закрывается PR со ссылкой; у каждого — как проверить, что он ещё открыт.

## Исполнители (portfolio/, btcalts/, carry/)

- **Ребалансировка в неделю запуска перезаписывала запись понедельника.** 14.09 второй прогон И14 заменил двуногую запись (ключ `rebalances.t` — понедельник); история спасена бэкапом. 15.09 добавлен журнал `rebalance_runs` (все прогоны), `rebalances` хранит последний. Долг: отчёты и API читают только `rebalances` — показать повторы из журнала.
  Verify: `grep -n rebalance_runs api/research_data.py portfolio/report.py` — пусто → открыт.
- **Демо-средства на пустом субсчёте — путь не проверен вживую.** Bybit выдал И18 100 000 USDT сам; `ensure_funds`/`apply_demo_usdt` покрыты только тестами с подделкой.
  Verify: `docker exec market-bot-btcalts python -c "import sqlite3; print(sqlite3.connect('/btcalts/btcalts.db').execute(\"select count(*) from events where kind='demo_funds'\").fetchone())"` → (0,) — путь не срабатывал.
- **Список не-крипты (`backtest/wide_search.NON_CRYPTO_BASES`) собран по именам** и неполон по построению (SKHYNIX всплыл 15.09). Динамическая блокировка по ошибке 110126 есть у И18 и ноги И14; в бэктестах новых имён не будет видно. Долг: источник признака «токенизированный актив» от биржи (если появится) или проверка кандидатов пробным запросом при формировании вселенной.
  Verify: `py -c "from backtest.wide_search import NON_CRYPTO_BASES as n; print(len(n))"` — список всё ещё ручной.
- **Час исполнения ребалансировок — 00:02 UTC понедельника, самый тонкий час** (атлас: 4 % суточного оборота против 7,6 % в 14:00). Решение — 12.10 по замеру проскальзывания четырёх ребалансировок; переносить вместе с бумагой.
- **Календарь уведомлений дублирует даты плана вручную** — тест `test_every_calendar_checkpoint_is_in_the_plan` ловит расхождение, но не обратное (дата в плане без календаря).

## Бэктесты (backtest/)

- **`history.sync_funding` перечитывает фандинг целиком при каждом повторе** (по замыслу — закрывает старые дыры); на 399 контрактах это ~45 мин. Долг: режим «только хвост» для докачки.
- **Загрузчик широкой вселенной живёт вне репо** (`scratchpad/wide-download.py`, `wide-download-1h.py`); кэш `history_wide.db` — локальный файл на машине владельца. Долг: `backtest/history_wide.py` с CLI и документация, где лежит кэш.
- **Просадка портфеля в `bar_rules` считается при неограниченном числе одновременных позиций** (10 % номинала × сотни контрактов → 300–1500 %). Для вердикта неважно (интервал не проходил нигде), для следующего правила по сделкам — ограничить число позиций.
- **PROGRAM в `api/research_data.py` повторяет тексты плана вручную** (сроки, критерии). Долг: генерировать из плана или хотя бы тест на совпадение дат.

## Мини-апп

- После v2 остались неиспользуемые части: зависимость `lightweight-charts`, `fetchMonthlyTarget`/`fetchAnalytics*` в `api/endpoints.ts`, роутеры `analytics`/`positions`/`signals` в API (фронт их не зовёт). Долг: убрать или пометить как «для Telegram-бота».
- Экраны проверены сборкой, типами и lint; глазами — только из Telegram (авторизация initData). Долг: тестовый режим с `DISABLE_AUTH` для просмотра в браузере на стенде.

## Данные

- **Разлоки и эмиссия токенов** — механизм «альты истекают к BTC» (атлас, И18) не подтверждён данными об предложении. Долг: еженедельная запись circulating supply по всем контрактам (CoinGecko) — 3–4 месяца до первой проверки.
